# macOS-exclusive CLI dependencies. Selected by platform from the deps engine;
# the linux equivalent is deps-linux.conf beside this file. Both ship on the
# single branch, selected at runtime. Format matches deps.conf.
#
# aerospace powers the mac Claude Code Stop-hook notification
# (~/.claude/hooks/notify.sh) -- see README-MAC.md.

aerospace|command -v aerospace|https://github.com/nikitabobko/AeroSpace
