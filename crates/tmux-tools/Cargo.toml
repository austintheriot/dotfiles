[package]
name = "tmux-tools"
edition = "2024"
version = "0.1.0"
publish = false

[dependencies]
tmux-core = { path = "../tmux-core" }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
